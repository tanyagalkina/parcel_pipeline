{
  "name": "parcel_pipeline",
  "source": "dev/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "spago build && parcel build && mkdir ./dist/assets && cp -r ./src/assets/* ./dist/assets"
  },
  "devDependencies": {
    "@parcel/compressor-brotli": "^2.12.0",
    "@parcel/compressor-gzip": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
