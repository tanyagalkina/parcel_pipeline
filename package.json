{
  "name": "parcel_pipeline",
  "source": "dev/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "npx spago build && parcel build dev/index.html"
  },

  "dependencies": {
    "spago": "0.21.0"
  },

  "devDependencies": {
    "purescript": "^0.15.15",
    "spago": "0.21.0",
    "@parcel/compressor-brotli": "^2.12.0",
    "@parcel/compressor-gzip": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
